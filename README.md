# Тестирование бинарного файла getfield16
Проект для автоматизированного тестирования бинарного файла getfield16 с использованием pytest и генерацией отчетов.

# Установка зависимостей
pip install -r requirements.txt

#Рализация getfield16
Размещение бинарного файла getfield16 в директории src/bin/
Программа реализации getfield16 на языке Python расположена в /src
